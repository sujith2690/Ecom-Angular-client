import { TruncateTextDirective } from './truncate-text.directive';

describe('TruncateTextDirective', () => {
  it('should create an instance', () => {
    const directive = new TruncateTextDirective();
    expect(directive).toBeTruthy();
  });
});
